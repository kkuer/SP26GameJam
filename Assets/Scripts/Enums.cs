using UnityEngine;

public enum NodeSlotState
{
    Occupied,
    Empty
}

public enum NodeState
{
    Draggable,
    Locked,
    InInventory
}

public enum SlotType
{
    Center,
    Abiility,
    Modifier
}